MDPIE BY ROBERT A SHAW (2016)

To setup, edit the Makefile to change the compiler settings and
locations of the boost libraries.

To run the program, see mdpie.py for an example. Otherwise,
the LJForces library can simply be imported into any python
program. 

